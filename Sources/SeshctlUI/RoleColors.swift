import SwiftUI

extension Color {
    /// Purple used for assistant/Claude role labels across all views.
    static let assistantPurple = Color(red: 0x93 / 255.0, green: 0x7C / 255.0, blue: 0xBF / 255.0)
}
